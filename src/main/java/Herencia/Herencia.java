package Herencia;

public final class Herencia {
  // Consideraciones

  // Una clase solo puede heredar de otra clase, aunque la superclase puede heredad a su vez a otra, y así hasta n niveles.
  // Varias clases pueden heredar de la misma clase
  // Los miebros privados de la superclase no son accesibles directamente de la superclase
}

// Todas las clases Java heredan Object
// Si una clase no hereda explicitamente otra clase, implicitamente heredará Object
// Todas las clases disponen de los métodos de Object, entre ellos: toString(), equals() y hashCode()

class clasePrueba {

}