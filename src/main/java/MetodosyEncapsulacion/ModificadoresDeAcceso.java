package MetodosyEncapsulacion;

public class ModificadoresDeAcceso {
  // Los modificadores de acceso determinan la visibilidad de los moienbros de una clase
  // La siguiente tabla indica los tipos de modificadores dónde se aplican
  // una clase nunca puede ser protected o privada



  // PUBLIC
  // Si una clase o miebro de la misma es public, pueden utilizarse desde cualquier clase que esté en su mismo paquete o en cualquier otro;


  // DEFAULT
  // Es el ámbito que se aplica cuando no se indica ningún modificador. El elemento que lo lleve solo es accesible desde clases de su mismo paquete


}
